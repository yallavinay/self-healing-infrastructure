groups:
  - name: service_down
    rules:
      - alert: NginxDown
        expr: up{job="nginx"} == 0
        for: 10s
        labels:
          severity: critical
        annotations:
          summary: "NGINX is down"
          description: "NGINX service is not responding for more than 10 seconds."
